import React from 'react';
import { View, Text} from 'react-native';

const RegisterScreen = () => {
    return (
        <View>
            <Text>Desde register screen</Text>
        </View>
    );
};

export default RegisterScreen